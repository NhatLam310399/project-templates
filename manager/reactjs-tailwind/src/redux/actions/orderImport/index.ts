import * as types from "redux/types/orderImport";

export const nextStepOrderImport = () => ({
  type: types.NEXT_STEP_ORDER_IMPORT,
});

export const defaultStepOrderImport = () => ({
  type: types.DEFAULT_STEP_ORDER_IMPORT,
});
