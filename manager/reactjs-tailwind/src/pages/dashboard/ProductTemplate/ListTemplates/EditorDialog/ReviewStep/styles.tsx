import styled from "styled-components";
import tw from "twin.macro";

export const DesignStepContainer = styled.div`
  ${tw``}
`;

export const ListActionButtons = styled.div`
  ${tw` flex flex-row gap-2 `}
`;
