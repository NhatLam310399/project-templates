import styled, { css } from "styled-components";
import tw from "twin.macro";

export const AllLayerContainer = styled.div`
  ${tw`mt-4`}
`;

export const ListObjects = styled.div`
  ${tw`flex flex-col overflow-y-auto bg-neutral-4 max-h-60`}
`;
