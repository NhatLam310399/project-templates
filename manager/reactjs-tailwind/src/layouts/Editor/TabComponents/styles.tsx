import styled from "styled-components";
import tw from "twin.macro";

export const BackToMenuContainer = styled.button`
  ${tw`flex flex-row items-center gap-1 mb-2 text-lg font-medium cursor-pointer text-primary-2 hover:underline`}
`;
