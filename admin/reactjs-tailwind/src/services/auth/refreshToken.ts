import { graphQLCommon } from "common/utils/api";
import { gql } from "apollo-boost";

// const REFRESH_TOKEN = gql``;

export const refreshToken = async (variables: null) => {
  // const response = await graphQLCommon(REFRESH_TOKEN, {});
  // return response;
  return {};
};
