import styled from "styled-components";
import tw from "twin.macro";

export const ContentContainer = styled.div`
  ${tw`inline-block overflow-hidden text-left align-middle transition-all transform bg-white shadow-xl `}
`;
